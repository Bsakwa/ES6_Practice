// Demonstrating the spread operator in JS
// Usage: node spreadop.js

const arr1 = "Hello";

const arr2 = [...arr1];

console.log(arr2); // [ 'H', 'e', 'l', 'l', 'o' ]

// Spread operator can be used to concatenate arrays
